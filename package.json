{
  "name": "tetoka",
  "version": "1.0.0",
  "scripts": {
    "dev": "concurrently \"cd server && nodemon\" \"cd client && npm run dev\"",
    "build": "cd client && npm install && npm run build && cd .. && cd server && npm install",
    "start": "cd server && ts-node main.ts"
  },
  "author": "Marcos Sousa",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^8.2.2"
  }
}
